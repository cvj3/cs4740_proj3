To run our code, simply execute "python test.py" from the command line.

This will parse the input files, build the relevant dictionaries, generate the predictions, and output them.

The predictions will be outputted in the results folder with the name "results.csv"